#include <stdio.h>
#define MAX 110

void moedas_bt(int vetorMoedas[MAX], int qtdMoedas, int valorCompra){
  if(k == qtdMoedas){

  }
  else{
    
  }
}

int int main(int argc, char const *argv[]) {
  int i, valorCompra, qtdMoedas, vetorMoedas[MAX];

  scanf("%d %d", &valorCompra, &qtdMoedas);

  for(i=0;i<qtdMoedas;i++)
    scanf("%d", &vetorMoedas[i]);

  moedas_bt(vetorMoedas[MAX], qtdMoedas, valorCompra);

  return 0;
}
