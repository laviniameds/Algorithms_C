#include <stdio.h>
int main(){
  int l, count = 0, resto;
  char *bin;
  scanf("%d", &l);
  while(l != 0){
    if(l = 1) printf("The parity of %d is %d (mod 2)", 1, 1);
    else if()
    while(l / 2 != 0){
      if(l%2 == 1){
        count++;
        bin += "1";
      }
      else bin += "0";
      l = l/2;
    }
  }
  return 0;
}
